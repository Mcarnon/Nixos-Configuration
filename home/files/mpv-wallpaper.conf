# mpvpaper 壁纸配置
# 基础设置
profile=wallpaper
loop=inf
no-audio
video-latency-hint=1

# 渲染设置
hwdec=auto-safe
gpu-api=vulkan
# 让 mpvpaper 正确铺满屏幕
keepaspect=no
# 禁用 OSC (on-screen controller)
osc=no
# 禁用光标
cursor=no

# 日志设置（调试用）
# log-file=/tmp/mpvpaper.log
