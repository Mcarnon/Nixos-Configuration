# mpvpaper 壁纸配置（mpv profile）
# 通过 mpvpaper -o "--profile=wallpaper" 加载

[wallpaper]
# 硬件解码与渲染
hwdec=auto-safe
gpu-api=vulkan

# 让视频铺满屏幕
keepaspect=no

# 禁用 OSC (on-screen controller) 与光标
osc=no
cursor=no

# 日志设置（调试用）
# log-file=/tmp/mpvpaper.log
